tools:
  - name: get_mesh_topology
    description: Summarize vertex, edge, and face counts for explicit mesh targets.
    source_file: action_get_mesh_topology.py
    execution: sync
    affinity: main
    enforce_thread_affinity: true
    input_schema:
      type: object
      additionalProperties: false
      properties:
        node_names: {type: array, items: {type: string, minLength: 1}}
        handles: {type: array, items: {type: integer}}
        use_selection: {type: boolean, default: false}
    output_schema:
      type: object
      additionalProperties: true
      properties:
        success: {type: boolean}
        status: {type: string}
        message: {type: string}
        data: {type: object}
    timeout_hint_secs: 10
    read_only: true
    destructive: false
    idempotent: true
    annotations:
      read_only_hint: true
      destructive_hint: false
      idempotent_hint: true
      open_world_hint: false

    intent: "Summarize vertex, edge, and face counts for mesh targets."
    tool_role: read_only
    risk: low
    search_aliases: ["get_mesh_topology", "get mesh topology", "mesh topology"]
    side_effects:
      creates: false
      modifies: false
      deletes: false
      exports: false
      imports: false
      file_output: false
      render: false
      targets: ['scene_nodes']
    produces: ['mesh_topology']
  - name: get_selected_mesh_topology
    description: Summarize topology for the current mesh selection.
    source_file: action_get_selected_mesh_topology.py
    execution: sync
    affinity: main
    enforce_thread_affinity: true
    input_schema:
      type: object
      additionalProperties: false
      properties: {}
    output_schema:
      type: object
      additionalProperties: true
      properties:
        success: {type: boolean}
        status: {type: string}
        message: {type: string}
        data: {type: object}
    timeout_hint_secs: 10
    read_only: true
    destructive: false
    idempotent: true
    annotations:
      read_only_hint: true
      destructive_hint: false
      idempotent_hint: true
      open_world_hint: false

    intent: "Summarize topology for the current mesh selection."
    tool_role: read_only
    risk: low
    search_aliases: ["get_selected_mesh_topology", "get selected mesh topology", "selected mesh topology"]
    side_effects:
      creates: false
      modifies: false
      deletes: false
      exports: false
      imports: false
      file_output: false
      render: false
      targets: []
    produces: ['mesh_topology']
  - name: get_smoothing_groups
    description: Summarize smoothing group usage for explicit mesh targets.
    source_file: action_get_smoothing_groups.py
    execution: sync
    affinity: main
    enforce_thread_affinity: true
    input_schema:
      type: object
      additionalProperties: false
      properties:
        node_names: {type: array, items: {type: string, minLength: 1}}
        handles: {type: array, items: {type: integer}}
        use_selection: {type: boolean, default: false}
    output_schema:
      type: object
      additionalProperties: true
      properties:
        success: {type: boolean}
        status: {type: string}
        message: {type: string}
        data: {type: object}
    timeout_hint_secs: 10
    read_only: true
    destructive: false
    idempotent: true
    annotations:
      read_only_hint: true
      destructive_hint: false
      idempotent_hint: true
      open_world_hint: false

    intent: "Summarize smoothing group usage on mesh targets."
    tool_role: read_only
    risk: low
    search_aliases: ["get_smoothing_groups", "get smoothing groups", "smoothing groups"]
    side_effects:
      creates: false
      modifies: false
      deletes: false
      exports: false
      imports: false
      file_output: false
      render: false
      targets: ['scene_nodes']
    produces: ['smoothing_groups']
  - name: get_modifier_stack
    description: >-
      Summarize modifier stack entries for explicit mesh targets, including each
      modifier's parameters and viewport/render enable state. Properties that
      cannot be read are reported in the entry's `unreadable` field.
    source_file: action_get_modifier_stack.py
    execution: sync
    affinity: main
    enforce_thread_affinity: true
    input_schema:
      type: object
      additionalProperties: false
      properties:
        node_names: {type: array, items: {type: string, minLength: 1}}
        handles: {type: array, items: {type: integer}}
        use_selection: {type: boolean, default: false}
        include_parameters: {type: boolean, default: true, description: Read each modifier's parameters into a `parameters` mapping.}
        property_names: {type: array, items: {type: string, minLength: 1}, description: Restrict the read to these modifier property names.}
    output_schema:
      type: object
      additionalProperties: true
      properties:
        success: {type: boolean}
        status: {type: string}
        message: {type: string}
        data: {type: object}
    timeout_hint_secs: 10
    read_only: true
    destructive: false
    idempotent: true
    annotations:
      read_only_hint: true
      destructive_hint: false
      idempotent_hint: true
      open_world_hint: false

    intent: "Summarize modifier stack entries and their parameters on mesh targets."
    tool_role: read_only
    risk: low
    search_aliases: ["get_modifier_stack", "get modifier stack", "modifier stack", "inspect_modifier_properties", "modifier parameters"]
    side_effects:
      creates: false
      modifies: false
      deletes: false
      exports: false
      imports: false
      file_output: false
      render: false
      targets: ['scene_nodes']
    produces: ['modifier_list', 'modifier_parameters']
  - name: add_modifier
    description: >-
      Add an arbitrary modifier class to explicit mesh targets by class name, with
      optional properties applied and verified after instantiation.
    source_file: action_add_modifier.py
    execution: sync
    affinity: main
    enforce_thread_affinity: true
    input_schema:
      type: object
      required: [modifier_class]
      additionalProperties: false
      properties:
        modifier_class: {type: string, minLength: 1, description: "3ds Max modifier class name, for example TurboSmooth, Bend, or Edit_Poly."}
        properties: {type: object, additionalProperties: true, description: Properties applied to the new modifier and verified by read-back.}
        node_names: {type: array, items: {type: string, minLength: 1}}
        handles: {type: array, items: {type: integer}}
        use_selection: {type: boolean, default: false}
    output_schema:
      type: object
      additionalProperties: true
      properties:
        success: {type: boolean}
        status: {type: string}
        message: {type: string}
        data: {type: object}
    timeout_hint_secs: 30
    read_only: false
    destructive: false
    idempotent: false
    annotations:
      read_only_hint: false
      destructive_hint: false
      idempotent_hint: false
      open_world_hint: false

    intent: "Add an arbitrary modifier class to mesh targets with optional verified properties."
    tool_role: action
    risk: medium
    search_aliases: ["add_modifier", "add modifier", "apply modifier"]
    side_effects:
      creates: true
      modifies: true
      deletes: false
      exports: false
      imports: false
      file_output: false
      render: false
      targets: ['scene_nodes', 'modifier']
    produces: ['modifier_stack']
  - name: remove_modifier
    description: >-
      Remove a modifier by name or 1-based index from explicit mesh targets. The
      modifier is resolved on every target before anything is removed.
    source_file: action_remove_modifier.py
    execution: sync
    affinity: main
    enforce_thread_affinity: true
    input_schema:
      type: object
      additionalProperties: false
      properties:
        node_names: {type: array, items: {type: string, minLength: 1}}
        handles: {type: array, items: {type: integer}}
        use_selection: {type: boolean, default: false}
        modifier_name: {type: string, minLength: 1}
        modifier_index: {type: integer, minimum: 1}
    output_schema:
      type: object
      additionalProperties: true
      properties:
        success: {type: boolean}
        status: {type: string}
        message: {type: string}
        data: {type: object}
    timeout_hint_secs: 30
    read_only: false
    destructive: true
    idempotent: false
    annotations:
      read_only_hint: false
      destructive_hint: true
      idempotent_hint: false
      open_world_hint: false

    intent: "Remove a modifier from mesh targets by name or index."
    tool_role: action
    risk: medium
    search_aliases: ["remove_modifier", "remove modifier", "delete modifier"]
    side_effects:
      creates: false
      modifies: true
      deletes: true
      exports: false
      imports: false
      file_output: false
      render: false
      targets: ['scene_nodes', 'modifier']
    produces: ['modifier_stack']
    undo:
      supported: true
      granularity: per_node
      notes: >-
        One host undo entry per node in the response; pass that count to `undo_last`.

  - name: set_modifier_state
    description: >-
      Enable or disable a modifier with separate viewport and render granularity.
      Every flag is verified by read-back.
    source_file: action_set_modifier_state.py
    execution: sync
    affinity: main
    enforce_thread_affinity: true
    input_schema:
      type: object
      additionalProperties: false
      properties:
        node_names: {type: array, items: {type: string, minLength: 1}}
        handles: {type: array, items: {type: integer}}
        use_selection: {type: boolean, default: false}
        modifier_name: {type: string, minLength: 1}
        modifier_index: {type: integer, minimum: 1}
        enabled: {type: boolean, description: Overall enable state of the modifier.}
        enabled_in_views: {type: boolean, description: Enable the modifier in the viewport only.}
        enabled_in_render: {type: boolean, description: Enable the modifier at render time only.}
    output_schema:
      type: object
      additionalProperties: true
      properties:
        success: {type: boolean}
        status: {type: string}
        message: {type: string}
        data: {type: object}
    timeout_hint_secs: 30
    read_only: false
    destructive: false
    idempotent: true
    annotations:
      read_only_hint: false
      destructive_hint: false
      idempotent_hint: true
      open_world_hint: false

    intent: "Enable or disable a modifier in the viewport and at render time."
    tool_role: action
    risk: low
    search_aliases: ["set_modifier_state", "set modifier state", "toggle modifier", "enable modifier", "disable modifier"]
    side_effects:
      creates: false
      modifies: true
      deletes: false
      exports: false
      imports: false
      file_output: false
      render: false
      targets: ['scene_nodes', 'modifier']
    produces: ['modifier_stack']
  - name: set_modifier_property
    description: >-
      Set properties on a modifier for one object or many. Each property is written,
      read back, and compared, so a rejected property fails the call.
    source_file: action_set_modifier_property.py
    execution: sync
    affinity: main
    enforce_thread_affinity: true
    input_schema:
      type: object
      required: [properties]
      additionalProperties: false
      properties:
        properties: {type: object, minProperties: 1, additionalProperties: true, description: Mapping of modifier property names to values.}
        node_names: {type: array, items: {type: string, minLength: 1}}
        handles: {type: array, items: {type: integer}}
        use_selection: {type: boolean, default: false}
        modifier_name: {type: string, minLength: 1}
        modifier_index: {type: integer, minimum: 1}
    output_schema:
      type: object
      additionalProperties: true
      properties:
        success: {type: boolean}
        status: {type: string}
        message: {type: string}
        data: {type: object}
    timeout_hint_secs: 30
    read_only: false
    destructive: false
    idempotent: true
    annotations:
      read_only_hint: false
      destructive_hint: false
      idempotent_hint: true
      open_world_hint: false

    intent: "Set verified properties on a modifier across one or many objects."
    tool_role: action
    risk: medium
    search_aliases: ["set_modifier_property", "set modifier property", "modifier parameters", "set modifier parameters"]
    side_effects:
      creates: false
      modifies: true
      deletes: false
      exports: false
      imports: false
      file_output: false
      render: false
      targets: ['scene_nodes', 'modifier']
    produces: ['modifier_stack']
  - name: collapse_modifier_stack
    description: >-
      Collapse the whole modifier stack of explicit mesh targets. Irreversible; the
      stack is re-read afterwards to confirm it actually shrank.
    source_file: action_collapse_modifier_stack.py
    execution: sync
    affinity: main
    enforce_thread_affinity: true
    input_schema:
      type: object
      additionalProperties: false
      properties:
        node_names: {type: array, items: {type: string, minLength: 1}}
        handles: {type: array, items: {type: integer}}
        use_selection: {type: boolean, default: false}
    output_schema:
      type: object
      additionalProperties: true
      properties:
        success: {type: boolean}
        status: {type: string}
        message: {type: string}
        data: {type: object}
    timeout_hint_secs: 60
    read_only: false
    destructive: true
    idempotent: false
    annotations:
      read_only_hint: false
      destructive_hint: true
      idempotent_hint: false
      open_world_hint: false

    intent: "Collapse the modifier stack of mesh targets into the base object."
    tool_role: action
    risk: high
    search_aliases: ["collapse_modifier_stack", "collapse modifier stack", "collapse stack"]
    side_effects:
      creates: false
      modifies: true
      deletes: true
      exports: false
      imports: false
      file_output: false
      render: false
      targets: ['scene_nodes', 'modifier']
    produces: ['scene_nodes']
    undo:
      supported: true
      granularity: per_node
      notes: >-
        Reversible only through the host stack: one entry per node, and the history is lost when the session ends.

  - name: make_modifier_unique
    description: >-
      Break modifier instancing so one node's modifier can be edited independently.
      Resolved on every target before anything is changed.
    source_file: action_make_modifier_unique.py
    execution: sync
    affinity: main
    enforce_thread_affinity: true
    input_schema:
      type: object
      additionalProperties: false
      properties:
        node_names: {type: array, items: {type: string, minLength: 1}}
        handles: {type: array, items: {type: integer}}
        use_selection: {type: boolean, default: false}
        modifier_name: {type: string, minLength: 1}
        modifier_index: {type: integer, minimum: 1}
    output_schema:
      type: object
      additionalProperties: true
      properties:
        success: {type: boolean}
        status: {type: string}
        message: {type: string}
        data: {type: object}
    timeout_hint_secs: 30
    read_only: false
    destructive: false
    idempotent: true
    annotations:
      read_only_hint: false
      destructive_hint: false
      idempotent_hint: true
      open_world_hint: false

    intent: "Make an instanced modifier unique on mesh targets."
    tool_role: action
    risk: medium
    search_aliases: ["make_modifier_unique", "make modifier unique", "de-instance modifier"]
    side_effects:
      creates: false
      modifies: true
      deletes: false
      exports: false
      imports: false
      file_output: false
      render: false
      targets: ['scene_nodes', 'modifier']
    produces: ['modifier_stack']
  - name: triangulate_meshes
    description: Triangulate explicit mesh targets using a reversible modifier where available.
    source_file: action_triangulate_meshes.py
    execution: sync
    affinity: main
    enforce_thread_affinity: true
    input_schema:
      type: object
      additionalProperties: false
      properties:
        node_names: {type: array, items: {type: string, minLength: 1}}
        handles: {type: array, items: {type: integer}}
        use_selection: {type: boolean, default: false}
    output_schema:
      type: object
      additionalProperties: true
      properties:
        success: {type: boolean}
        status: {type: string}
        message: {type: string}
        data: {type: object}
    timeout_hint_secs: 30
    read_only: false
    destructive: false
    idempotent: false
    annotations:
      read_only_hint: false
      destructive_hint: false
      idempotent_hint: false
      open_world_hint: false

    intent: "Triangulate mesh targets using a reversible modifier."
    tool_role: action
    risk: medium
    search_aliases: ["triangulate_meshes", "triangulate meshes", "triangulate meshes"]
    side_effects:
      creates: false
      modifies: true
      deletes: false
      exports: false
      imports: false
      file_output: false
      render: false
      targets: ['scene_nodes']
    produces: ['scene_nodes']
  - name: cleanup_meshes
    description: Run focused cleanup checks on explicit mesh targets.
    source_file: action_cleanup_meshes.py
    execution: sync
    affinity: main
    enforce_thread_affinity: true
    input_schema:
      type: object
      additionalProperties: false
      properties:
        node_names: {type: array, items: {type: string, minLength: 1}}
        handles: {type: array, items: {type: integer}}
        use_selection: {type: boolean, default: false}
        weld_threshold: {type: number, minimum: 0, description: Optional weld threshold for hosts that support it.}
    output_schema:
      type: object
      additionalProperties: true
      properties:
        success: {type: boolean}
        status: {type: string}
        message: {type: string}
        data: {type: object}
    timeout_hint_secs: 30
    read_only: false
    destructive: false
    idempotent: false
    annotations:
      read_only_hint: false
      destructive_hint: false
      idempotent_hint: false
      open_world_hint: false

    intent: "Run focused cleanup checks like welding on mesh targets."
    tool_role: action
    risk: medium
    search_aliases: ["cleanup_meshes", "cleanup meshes", "cleanup meshes"]
    side_effects:
      creates: false
      modifies: true
      deletes: false
      exports: false
      imports: false
      file_output: false
      render: false
      targets: ['scene_nodes']
    produces: ['scene_nodes']
  - name: attach_meshes
    description: Attach source meshes into one explicit target mesh.
    source_file: action_attach_meshes.py
    execution: sync
    affinity: main
    enforce_thread_affinity: true
    input_schema:
      type: object
      additionalProperties: false
      properties:
        target_name: {type: string, minLength: 1}
        target_handle: {type: integer}
        source_names: {type: array, items: {type: string, minLength: 1}}
        source_handles: {type: array, items: {type: integer}}
    output_schema:
      type: object
      additionalProperties: true
      properties:
        success: {type: boolean}
        status: {type: string}
        message: {type: string}
        data: {type: object}
    timeout_hint_secs: 30
    read_only: false
    destructive: false
    idempotent: false
    annotations:
      read_only_hint: false
      destructive_hint: false
      idempotent_hint: false
      open_world_hint: false

    intent: "Attach source meshes into one explicit target mesh."
    tool_role: action
    risk: medium
    search_aliases: ["attach_meshes", "attach meshes", "attach meshes"]
    side_effects:
      creates: false
      modifies: true
      deletes: false
      exports: false
      imports: false
      file_output: false
      render: false
      targets: ['scene_nodes']
    produces: ['scene_nodes']
  - name: detach_selected_faces
    description: Detach explicit faces or the current face selection into a separate node.
    source_file: action_detach_selected_faces.py
    execution: sync
    affinity: main
    enforce_thread_affinity: true
    input_schema:
      type: object
      additionalProperties: false
      properties:
        node_name: {type: string, minLength: 1}
        handle: {type: integer}
        face_indices: {type: array, items: {type: integer, minimum: 1}}
        use_current_face_selection: {type: boolean, default: false}
        detach_name: {type: string, minLength: 1, default: DetachedMesh}
    output_schema:
      type: object
      additionalProperties: true
      properties:
        success: {type: boolean}
        status: {type: string}
        message: {type: string}
        data: {type: object}
    timeout_hint_secs: 30
    read_only: false
    destructive: false
    idempotent: false
    annotations:
      read_only_hint: false
      destructive_hint: false
      idempotent_hint: false
      open_world_hint: false

    intent: "Detach faces into a separate mesh node."
    tool_role: action
    risk: medium
    search_aliases: ["detach_selected_faces", "detach selected faces", "detach selected faces"]
    side_effects:
      creates: false
      modifies: true
      deletes: false
      exports: false
      imports: false
      file_output: false
      render: false
      targets: ['scene_nodes']
    produces: ['scene_nodes']
  - name: apply_subdivision
    description: Add a subdivision modifier to explicit mesh targets.
    source_file: action_apply_subdivision.py
    execution: sync
    affinity: main
    enforce_thread_affinity: true
    input_schema:
      type: object
      additionalProperties: false
      properties:
        node_names: {type: array, items: {type: string, minLength: 1}}
        handles: {type: array, items: {type: integer}}
        use_selection: {type: boolean, default: false}
        iterations: {type: integer, minimum: 0, maximum: 6, default: 1}
        render_iterations: {type: integer, minimum: 0, maximum: 6}
    output_schema:
      type: object
      additionalProperties: true
      properties:
        success: {type: boolean}
        status: {type: string}
        message: {type: string}
        data: {type: object}
    timeout_hint_secs: 30
    read_only: false
    destructive: false
    idempotent: false
    annotations:
      read_only_hint: false
      destructive_hint: false
      idempotent_hint: false
      open_world_hint: false

    intent: "Add a subdivision modifier with configurable iterations."
    tool_role: action
    risk: medium
    search_aliases: ["apply_subdivision", "apply subdivision", "apply subdivision"]
    side_effects:
      creates: false
      modifies: true
      deletes: false
      exports: false
      imports: false
      file_output: false
      render: false
      targets: ['scene_nodes']
    produces: ['scene_nodes']
  - name: create_proxy_meshes
    description: Duplicate explicit meshes and add a proxy reduction modifier where available.
    source_file: action_create_proxy_meshes.py
    execution: sync
    affinity: main
    enforce_thread_affinity: true
    input_schema:
      type: object
      additionalProperties: false
      properties:
        node_names: {type: array, items: {type: string, minLength: 1}}
        handles: {type: array, items: {type: integer}}
        use_selection: {type: boolean, default: false}
        reduction_percent: {type: number, minimum: 1, maximum: 100, default: 50}
        name_suffix: {type: string, default: _proxy}
    output_schema:
      type: object
      additionalProperties: true
      properties:
        success: {type: boolean}
        status: {type: string}
        message: {type: string}
        data: {type: object}
    timeout_hint_secs: 30
    read_only: false
    destructive: false
    idempotent: false
    annotations:
      read_only_hint: false
      destructive_hint: false
      idempotent_hint: false
      open_world_hint: false

    intent: "Duplicate meshes and add a proxy reduction modifier."
    tool_role: action
    risk: medium
    search_aliases: ["create_proxy_meshes", "create proxy meshes", "proxy meshes"]
    side_effects:
      creates: true
      modifies: false
      deletes: false
      exports: false
      imports: false
      file_output: false
      render: false
      targets: ['scene_nodes']
    produces: ['scene_nodes']
  - name: set_explicit_normals
    description: Set explicit normal data for explicit mesh targets.
    source_file: action_set_explicit_normals.py
    execution: sync
    affinity: main
    enforce_thread_affinity: true
    input_schema:
      type: object
      required: [normal]
      additionalProperties: false
      properties:
        node_names: {type: array, items: {type: string, minLength: 1}}
        handles: {type: array, items: {type: integer}}
        use_selection: {type: boolean, default: false}
        normal:
          type: array
          minItems: 3
          maxItems: 3
          items: {type: number}
    output_schema:
      type: object
      additionalProperties: true
      properties:
        success: {type: boolean}
        status: {type: string}
        message: {type: string}
        data: {type: object}
    timeout_hint_secs: 30
    read_only: false
    destructive: false
    idempotent: false
    annotations:
      read_only_hint: false
      destructive_hint: false
      idempotent_hint: false
      open_world_hint: false

    intent: "Set explicit normal vectors on mesh targets."
    tool_role: action
    risk: medium
    search_aliases: ["set_explicit_normals", "set explicit normals", "explicit normals"]
    side_effects:
      creates: false
      modifies: true
      deletes: false
      exports: false
      imports: false
      file_output: false
      render: false
      targets: ['scene_nodes']
    produces: ['scene_nodes']
  - name: clear_explicit_normals
    description: Clear explicit normal data for explicit mesh targets.
    source_file: action_clear_explicit_normals.py
    execution: sync
    affinity: main
    enforce_thread_affinity: true
    input_schema:
      type: object
      additionalProperties: false
      properties:
        node_names: {type: array, items: {type: string, minLength: 1}}
        handles: {type: array, items: {type: integer}}
        use_selection: {type: boolean, default: false}
    output_schema:
      type: object
      additionalProperties: true
      properties:
        success: {type: boolean}
        status: {type: string}
        message: {type: string}
        data: {type: object}
    timeout_hint_secs: 30
    read_only: false
    destructive: false
    idempotent: false
    annotations:
      read_only_hint: false
      destructive_hint: false
      idempotent_hint: false
      open_world_hint: false

    intent: "Clear explicit normal data on mesh targets."
    tool_role: action
    risk: medium
    search_aliases: ["clear_explicit_normals", "clear explicit normals", "clear explicit normals"]
    side_effects:
      creates: false
      modifies: true
      deletes: false
      exports: false
      imports: false
      file_output: false
      render: false
      targets: ['scene_nodes']
    produces: ['scene_nodes']
  - name: assign_smoothing_group
    description: Assign a smoothing group to explicit mesh targets and optional face indices.
    source_file: action_assign_smoothing_group.py
    execution: sync
    affinity: main
    enforce_thread_affinity: true
    input_schema:
      type: object
      required: [smoothing_group]
      additionalProperties: false
      properties:
        node_names: {type: array, items: {type: string, minLength: 1}}
        handles: {type: array, items: {type: integer}}
        use_selection: {type: boolean, default: false}
        smoothing_group: {type: integer, minimum: 1, maximum: 32}
        face_indices: {type: array, items: {type: integer, minimum: 1}}
    output_schema:
      type: object
      additionalProperties: true
      properties:
        success: {type: boolean}
        status: {type: string}
        message: {type: string}
        data: {type: object}
    timeout_hint_secs: 30
    read_only: false
    destructive: false
    idempotent: false
    annotations:
      read_only_hint: false
      destructive_hint: false
      idempotent_hint: false
      open_world_hint: false

    intent: "Assign a smoothing group number to mesh targets."
    tool_role: action
    risk: medium
    search_aliases: ["assign_smoothing_group", "assign smoothing group", "assign smoothing group"]
    side_effects:
      creates: false
      modifies: true
      deletes: false
      exports: false
      imports: false
      file_output: false
      render: false
      targets: ['scene_nodes']
    produces: ['scene_nodes']
